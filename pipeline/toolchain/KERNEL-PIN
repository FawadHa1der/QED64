992dc94b2de491878cca61aca382ed31286f3a86  qed64-wasm64 @ FawadHa1der/lean4
# Patch series through 0032 (fork work/lean4 HEAD c957526): resident header
# resolver + one environment registry + generated exports. Bump deliberately;
# a stage1 rebuild from a new pin REQUIRES rebaking the work/snapshot pairing
# and, for served artifacts, the full promote flow (pipeline/release/promote-staging.mjs).
#
# Paired build identity (the other half of the invariant — a stage1 rebuilt
# from any other commit UNPAIRS these and the compiler battery breaks):
#   runtime        wasm64-5dcdda005a7c5ae0 (sha256[:16] of stage1/bin/lean.wasm)
#   mathlib.snap   1114420853 bytes raw (probe: import QED64.Essential, lib-tree-slim)
#   init.snap      121481845 bytes raw (lib: core-lib-slim)
#   staging        work/staging/wasm64-5dcdda005a7c5ae0/{runtime,snapshots} (index entries carry `runtime`)
