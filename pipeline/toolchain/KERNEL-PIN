8d91aadcda8a0b231e47c42d4ff4b9b8360563aa  qed64-wasm64 @ FawadHa1der/lean4
# Lean 4.34.0: upstream tag v4.34.0 merged over the wasm64 series (resident
# transport 0031, resolver 0032, pump entry points retired 0033, exports
# generated as boxed ∪ initializers ∪ cells). This is the commit that BUILT
# the served binary (the kernel repo's wasm64-build/ lane; later commits on
# the branch touch scripts and docs only). Bump deliberately: a stage1
# rebuild from a new pin REQUIRES rebaking the snapshot pairing and, for
# served artifacts, the full flow (pipeline/release/import-packs.sh for a
# version import, pipeline/release/bump-chain.sh stage → pyramid → promote).
#
# Paired build identity (the other half of the invariant — a stage1 rebuilt
# from any other commit UNPAIRS these and the compiler battery breaks):
#   runtime        wasm64-36a96239e08fd2e0 (sha256[:16] of stage1/bin/lean.wasm, 109,869,533 bytes)
#   mathlib.snap   1127273069 bytes raw (probe: import QED64.Essential, lib-tree-slim; Mathlib 5ed2965 = v4.34.0, 4,354 modules incl. 80 deprecated shims)
#   init.snap      122364149 bytes raw (lib: core-lib-slim, 649 modules)
#   packs          lean-core 1c5c75db… (390,065,102 B), mathlib-essential eeab078f… (3,568,001,947 B) — promoted with the runtime
#   staging        work/staging/wasm64-36a96239e08fd2e0/{runtime,snapshots,profiles} (index entries carry `runtime`)
