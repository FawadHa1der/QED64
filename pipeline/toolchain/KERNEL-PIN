37c3fc3adf7941265fad4ae0ce134827a14d26a1  qed64-wasm64 @ FawadHa1der/lean4
# Patch series through 0033 (fork work/lean4 HEAD a437a4c): resident header
# resolver + one environment registry + generated exports (0032), and the
# host-pumped LSP entry points retired (0033) — the resident transport is the
# only transport. Bump deliberately; a stage1 rebuild from a new pin REQUIRES
# rebaking the work/snapshot pairing and, for served artifacts, the full
# promote flow (pipeline/release/bump-chain.sh stage → pyramid → promote).
#
# Paired build identity (the other half of the invariant — a stage1 rebuilt
# from any other commit UNPAIRS these and the compiler battery breaks):
#   runtime        wasm64-c645477e817ac857 (sha256[:16] of stage1/bin/lean.wasm, 105,449,148 bytes)
#   mathlib.snap   1114420853 bytes raw (probe: import QED64.Essential, lib-tree-slim)
#   init.snap      121481845 bytes raw (lib: core-lib-slim)
#   staging        work/staging/wasm64-c645477e817ac857/{runtime,snapshots} (index entries carry `runtime`)
